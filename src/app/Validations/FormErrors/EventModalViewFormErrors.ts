import { Injectable } from '@angular/core';
import { FormGroup } from '@angular/forms';
import { BaseFormErrors } from './BaseFormErrors';

@Injectable({
  providedIn: 'root',
})
export class EventModalViewFormErrors extends BaseFormErrors {}
